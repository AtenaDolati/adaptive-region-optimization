[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "arrgo"
version = "0.1.0"
description = "Adaptive Region Refinement for Global Optimization"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]